---
import Base from '../../layouts/Base.astro';
import Legal from '../../components/Legal.astro';
import { t, privacyHrefs, homeHrefs } from '../../data/content';

const lang = 'es';
---

<Base lang={lang} t={t[lang]} alternates={privacyHrefs} title={`${t[lang].privacy.title} — Sebastian Cepeda`}>
  <Legal t={t[lang]} homeHref={homeHrefs[lang]} />
</Base>
